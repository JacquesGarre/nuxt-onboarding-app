{% extends 'base.html.twig' %}

{% block title %}Friendship{% endblock %}

{% block body %}
    <h1>Friendship</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ friendship.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ friendship.createdAt ? friendship.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_friendship_index') }}">back to list</a>

    <a href="{{ path('app_friendship_edit', {'id': friendship.id}) }}">edit</a>

    {{ include('friendship/_delete_form.html.twig') }}
{% endblock %}
