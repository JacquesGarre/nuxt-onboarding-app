{% extends 'base.html.twig' %}

{% block title %}UserNode{% endblock %}

{% block body %}
    <h1>UserNode</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_node.id }}</td>
            </tr>
            <tr>
                <th>Longitude</th>
                <td>{{ user_node.longitude }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ user_node.latitude }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ user_node.createdAt ? user_node.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_node_index') }}">back to list</a>

    <a href="{{ path('app_user_node_edit', {'id': user_node.id}) }}">edit</a>

    {{ include('user_node/_delete_form.html.twig') }}
{% endblock %}
