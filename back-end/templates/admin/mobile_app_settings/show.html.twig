{% extends 'base.html.twig' %}

{% block title %}MobileAppSettings{% endblock %}

{% block body %}
    <h1>MobileAppSettings</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mobile_app_setting.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ mobile_app_setting.label }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ mobile_app_setting.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mobile_app_settings_index') }}">back to list</a>

    <a href="{{ path('app_mobile_app_settings_edit', {'id': mobile_app_setting.id}) }}">edit</a>

    {{ include('mobile_app_settings/_delete_form.html.twig') }}
{% endblock %}
