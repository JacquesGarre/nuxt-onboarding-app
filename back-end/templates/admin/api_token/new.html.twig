{% extends 'admin/index.html.twig' %}

{% block title %}New token{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-xl">
            <div class="card mb-4">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h5 class="mb-0">Create new token</h5>
                    <a class="btn btn-primary float-end" href="{{ path('app_api_token_index') }}"><i class="menu-icon tf-icons bx bx-caret-left"></i> Back to list</a>
                </div>
                <div class="card-body">
                    {{ include('admin/api_token/_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}